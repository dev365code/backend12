# Redis 설정 파일
bind 0.0.0.0
port 6379

# 메모리 설정
maxmemory 512mb
maxmemory-policy allkeys-lru

# 데이터 지속성
appendonly yes
appendfsync everysec

# 로그 설정
loglevel notice
logfile ""

# 보안 설정 (선택사항)
# requirepass your_redis_password

# 성능 튜닝
tcp-keepalive 300
timeout 0

# 백그라운드 저장 비활성화 (AOF 사용하므로)
save ""